"""PyQt6 front-end for PyMORGAN.

This subpackage is a thin Qt layer over the framework-agnostic ``pymorgan``
package: it never adds anything ``pymorgan`` depends on. Qt is only required
here, behind the ``[gui]`` extra. Launch with ``pymorgan-gui`` or
``python -m pymorgan.gui``.
"""
